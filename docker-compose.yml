# docker-compose.yml
services:
  freebuff2api:
    image: yflwz/freebuff2api:latest
    container_name: freebuff2api
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./.env:/app/.env
